version: "3.8"

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    image: ryanzola-dev
    container_name: ryanzola-dev
    ports:
      - "5173:5173"
    volumes:
      - .:/app
      - /app/node_modules
